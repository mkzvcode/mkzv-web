import Link from "next/link";
import { ArtworkSlot } from "@/components/ArtworkSlot";
import { Footer } from "@/components/Footer";
import { Header } from "@/components/Header";
import { ProjectRow } from "@/components/ProjectRow";
import { SectionLabel } from "@/components/SectionLabel";
import {
  audiences,
  deliverables,
  faqItems,
  marketEdges,
  offerPoints,
  packages,
  processSteps,
  projects,
  services,
  trustItems,
} from "@/data/projects";

export default function Home() {
  return <><Header /><main className="page-shell page-grid border-x border-[var(--line)]">
    <section className="home-hero product-hero grid gap-8 px-4 py-8 md:px-6 md:py-10 xl:grid-cols-[minmax(0,0.9fr)_minmax(420px,0.72fr)]">
      <div className="flex min-w-0 flex-col justify-between gap-12"><div><p className="mono mb-7 text-[var(--muted)]">01. Home / sites that sell / full-stack</p><h1 className="display hero-title home-title">MKZV<br />WEB</h1><p className="copy-lg mt-8 max-w-2xl text-[var(--muted)]">Делаю сайты, которые выглядят дорого, быстро объясняют ценность и ведут человека к заявке. Дизайн, текстовая структура, frontend и запуск — в одной связке.</p><div className="hero-proof mt-8 grid gap-3 sm:grid-cols-3"><p className="mono">Сайт под ключ</p><p className="mono">Мобильная версия</p><p className="mono">Форма / аналитика</p></div></div><div className="grid max-w-2xl gap-3 sm:grid-cols-2"><Link className="thin-button" href="/contacts">Обсудить сайт <span aria-hidden="true">→</span></Link><Link className="thin-button" href="/projects">Смотреть подход <span aria-hidden="true">→</span></Link></div></div>
      <div className="hero-artwork self-stretch"><ArtworkSlot className="hero-sphere" wide /><p className="hero-artwork-note mono">Noise sphere / live loop<br />MKZV WEB / 2026</p></div>
    </section>
    <section className="section px-4 md:px-6"><SectionLabel index="02" title="Что покупают" /><div className="grid gap-3 lg:grid-cols-2">{services.map((service) => <article className="service-card panel min-w-0" data-index={service.meta} key={service.title}><p className="mono mb-10 text-[var(--muted)]">[{service.meta}]</p><h2 className="display service-title">{service.title}</h2><p className="copy mt-6 max-w-xl text-[var(--muted)]">{service.text}</p></article>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="03" title="Кому подойдёт" /><div className="audience-grid grid gap-3 md:grid-cols-2 xl:grid-cols-4">{audiences.map((item, index) => <article className="audience-card panel min-w-0 p-5" key={item.title}><p className="mono text-[var(--muted)]">0{index + 1} / клиент</p><h2 className="display mt-9 text-[clamp(1.55rem,2.2vw,2.75rem)]">{item.title}</h2><p className="copy mt-5 text-[var(--muted)]">{item.text}</p></article>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="04" title="Почему это работает" /><div className="grid gap-3 lg:grid-cols-3">{offerPoints.map((point, index) => <article className="sell-card panel min-w-0 p-5" key={point.title}><p className="mono text-[var(--muted)]">0{index + 1} / отличие</p><h2 className="display mt-8 text-[clamp(1.45rem,2vw,2.35rem)]">{point.title}</h2><p className="copy mt-5 text-[var(--muted)]">{point.text}</p></article>)}</div><div className="trust-strip mt-3 grid gap-3 lg:grid-cols-4">{trustItems.map((item) => <p className="mono panel min-w-0 p-4 leading-5 text-[var(--paper)]" key={item}>{item}</p>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="05" title="Не как у всех" /><div className="market-grid grid gap-3 lg:grid-cols-3">{marketEdges.map((edge) => <article className="market-card panel min-w-0 p-5" key={edge.label}><p className="mono text-[var(--paper)]">{edge.label}</p><div className="mt-8 grid gap-5"><p className="copy text-[var(--muted)]"><span className="mono block text-[var(--muted-2)]">Обычно</span>{edge.pain}</p><p className="copy text-[var(--paper)]"><span className="mono block text-[var(--muted-2)]">MKZV WEB</span>{edge.mkzv}</p></div></article>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="06" title="Избранные проекты" /><div className="grid gap-3">{projects.map((project) => <ProjectRow key={project.id} project={project} />)}</div><Link className="thin-button mt-4 w-full" href="/projects">Показать все проекты <span aria-hidden="true">→</span></Link></section>
    <section className="section px-4 md:px-6"><SectionLabel index="07" title="Что входит" /><div className="grid gap-3 lg:grid-cols-4">{deliverables.map((item, index) => <article className="deliverable-card panel min-w-0 p-5" key={item.title}><p className="mono text-[var(--muted)]">0{index + 1}</p><h2 className="display mt-10 text-[clamp(1.45rem,2vw,2.3rem)]">{item.title}</h2><p className="copy mt-5 text-[var(--muted)]">{item.text}</p></article>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="08" title="Форматы" /><div className="grid gap-3 lg:grid-cols-3">{packages.map((item, index) => <article className="package-card panel min-w-0 p-5" key={item.title}><p className="mono text-[var(--muted)]">0{index + 1} / формат</p><h2 className="display mt-8 text-[clamp(1.7rem,2.45vw,3rem)]">{item.title}</h2><p className="copy mt-4 text-[var(--muted)]">{item.subtitle}</p><ul className="mt-8 grid gap-3">{item.items.map((point) => <li className="mono package-point" key={point}>{point}</li>)}</ul></article>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="09" title="Процесс" /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{processSteps.map((step, index) => <div className="process-card panel p-5" data-index={`0${index + 1}`} key={step}><p className="mono text-[var(--muted)]">0{index + 1}</p><p className="display mt-14 text-[clamp(1.55rem,2.2vw,2.65rem)]">{step}</p></div>)}</div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="10" title="Как стартуем" /><div className="grid gap-3 lg:grid-cols-3"><article className="panel min-w-0 p-5"><p className="mono text-[var(--muted)]">01 / 20 минут</p><h2 className="display mt-8 text-[clamp(1.7rem,2.5vw,2.8rem)]">Быстрый<br />разбор</h2><p className="copy mt-6 text-[var(--muted)]">Ты пишешь, чем занимаешься, какой сайт нужен и куда должен приходить клиент.</p></article><article className="panel min-w-0 p-5"><p className="mono text-[var(--muted)]">02 / План</p><h2 className="display mt-8 text-[clamp(1.7rem,2.5vw,2.8rem)]">Собираю<br />маршрут</h2><p className="copy mt-6 text-[var(--muted)]">Предлагаю структуру страниц, ключевые блоки, формат запуска и честную вилку по объему.</p></article><article className="panel min-w-0 p-5"><p className="mono text-[var(--muted)]">03 / Работа</p><h2 className="display mt-8 text-[clamp(1.7rem,2.5vw,2.8rem)]">Делаю<br />и веду</h2><p className="copy mt-6 text-[var(--muted)]">Показываю промежуточные версии, правлю по делу и довожу сайт до публикации.</p></article></div></section>
    <section className="section px-4 md:px-6"><SectionLabel index="11" title="Вопросы" /><div className="grid gap-3 lg:grid-cols-3">{faqItems.map((item, index) => <article className="faq-card panel min-w-0 p-5" key={item.question}><p className="mono text-[var(--muted)]">0{index + 1} / вопрос</p><h2 className="display mt-8 text-[clamp(1.25rem,1.75vw,2.1rem)]">{item.question}</h2><p className="copy mt-5 text-[var(--muted)]">{item.answer}</p></article>)}</div></section>
    <section className="signal-teaser section relative overflow-hidden px-4 md:px-6"><ArtworkSlot className="teaser-sphere" /><div className="relative z-10 grid gap-8 xl:grid-cols-[minmax(0,0.72fr)_minmax(320px,0.44fr)]"><div><p className="mono mb-5 text-[var(--muted)]">12. Signal / side project</p><h2 className="display signal-title">Noise in<br />the system</h2></div><div className="self-end"><p className="copy-lg text-[var(--muted)]">Музыка, биты и шум — отдельный слой MKZV. Он добавляет сайту характер и показывает, что визуальный язык здесь не из шаблона.</p><Link className="thin-button mt-7" href="/signal">Открыть Signal <span aria-hidden="true">→</span></Link></div></div></section>
    <section className="home-cta grid gap-7 px-4 py-10 md:grid-cols-[minmax(0,1fr)_auto] md:px-6"><p className="display case-card-title">Нужен сайт,<br />который продаёт?</p><Link className="thin-button self-end" href="/contacts">Написать о проекте <span aria-hidden="true">→</span></Link></section>
  </main><Footer /></>;
}
